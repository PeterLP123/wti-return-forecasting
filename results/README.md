# Results

Runtime outputs are written here and ignored by Git.

- `nll_grid/`: incremental grid-search tables, best-parameter JSON files, model checkpoints, training curves, predictions, and the final comparison table.
- Notebook-level CSV and figure exports may also be created during exploratory runs.

The reviewed, publication-ready outputs are the notebook outputs and the assets under `report/`.
